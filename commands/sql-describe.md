---
description: Describe a SQL Server table (columns, types, keys)
argument-hint: <[schema.]table_name>
---

Use the `sqlserver` MCP server tools to describe `$ARGUMENTS`: list its columns/types via describe-table, and include foreign keys and row stats if available. Present as a concise markdown summary.
