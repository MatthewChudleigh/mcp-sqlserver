{
  "mcpServers": {
    "sqlserver": {
      "command": "npx",
      "args": ["-y", "@ibergeni/mcp-sqlserver"],
      "env": {
        "SQLSERVER_CONFIG_FILE": "${SQLSERVER_CONFIG_FILE:-~/.config/mcp-sqlserver/connections.yaml}"
      }
    }
  }
}
